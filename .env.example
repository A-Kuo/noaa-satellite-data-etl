# Copy this file to .env and fill in real values before running the pipeline.

# PostgreSQL / PostGIS connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=noaa_dw
DB_USER=postgres
DB_PASSWORD=postgres

# NOAA API
NOAA_BASE_URL=https://storms.ngs.noaa.gov/storms
NOAA_REQUEST_TIMEOUT=30
NOAA_MAX_RETRIES=3

# Storage paths (relative to repo root; must NOT be committed)
STAGING_DIR=data/staging
ARCHIVE_DIR=data/archive

# Pipeline
BATCH_SIZE=100
LOG_LEVEL=INFO
