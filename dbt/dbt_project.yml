name: noaa_dw
version: "1.0.0"
config-version: 2

profile: noaa_dw

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  noaa_dw:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
